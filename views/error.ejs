<!doctype html>
<html>

<head>
  <%- include("partials/head") %>
  <title>Erro - SQMCC</title>
</head>

<body class="page-enter bg-bg-main text-text-main flex flex-col min-h-screen">
  <%- include("partials/nav", { user: null }) %>
  <main class="flex-grow flex items-center justify-center p-6">
    <div class="glass rounded-2xl shadow-soft border border-white/60 p-10 max-w-md w-full text-center">
      <div class="w-14 h-14 mx-auto rounded-2xl bg-red-100 text-red-600 flex items-center justify-center font-bold text-2xl">!</div>
      <h1 class="headline text-2xl font-bold text-text-main mt-6">Ocorreu um erro</h1>
      <p class="text-text-meta mt-3"><%= message %></p>
      <a href="/"
        class="inline-flex mt-6 px-6 py-2 rounded-xl bg-primary text-white font-semibold hover:bg-teal-700 shadow-glow transition-colors">Voltar para a Home</a>
    </div>
  </main>
</body>

</html>

