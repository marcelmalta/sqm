<nav class="w-full bg-bg-surface/80 border-b border-white/5 shadow-soft sticky top-0 z-50 backdrop-blur">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex items-center">
        <a href="/" class="flex-shrink-0 flex items-center gap-3">
          <img src="/images/logo-horizontal.png" alt="SQM Brasil" class="h-8 md:h-9 w-auto" />
        </a>
      </div>

      <div class="hidden md:flex md:items-center md:space-x-8">
        <a href="/" class="text-text-main hover:text-primary px-3 py-2 rounded-md text-sm font-semibold transition-colors">Home</a>
        <a href="/forum" class="text-text-main hover:text-primary px-3 py-2 rounded-md text-sm font-semibold transition-colors">Comunidade</a>
        <a href="/cart" class="text-text-main hover:text-primary px-3 py-2 rounded-md text-sm font-semibold transition-colors">Carrinho</a>
        <% if (user?.role==='admin' ) { %>
          <a href="/admin/moderation" class="text-text-main hover:text-primary px-3 py-2 rounded-md text-sm font-semibold transition-colors">Moderação</a>
          <a href="/admin/new-post" class="text-text-main hover:text-primary px-3 py-2 rounded-md text-sm font-semibold transition-colors">Novo Post</a>
          <div class="ml-4 flex items-center gap-4">
            <span class="text-xs text-text-meta"><%= user.email %></span>
            <form method="post" action="/admin/logout" class="inline">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <button class="bg-bg-card hover:bg-bg-surface text-text-main px-4 py-2 rounded-lg text-sm font-semibold transition-colors border border-white/10">Sair Admin</button>
            </form>
          </div>
        <% } else { %>
          <div class="ml-4 flex items-center gap-3">
            <a href="/admin/login" class="text-text-main hover:text-primary px-3 py-2 rounded-md text-sm font-semibold transition-colors">Admin</a>
          </div>
        <% } %>
      </div>

      <div class="flex items-center md:hidden">
        <button type="button" onclick="document.getElementById('mobile-menu').classList.toggle('hidden')"
          class="inline-flex items-center justify-center p-2 rounded-md text-text-meta hover:text-text-main hover:bg-bg-card focus:outline-none focus:ring-2 focus:ring-inset focus:ring-primary"
          aria-controls="mobile-menu" aria-expanded="false">
          <span class="sr-only">Abrir menu</span>
          <svg class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
            stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <div class="hidden md:hidden bg-bg-surface/90 border-t border-white/5" id="mobile-menu">
    <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
      <a href="/" class="text-text-main hover:text-primary hover:bg-bg-card block px-3 py-2 rounded-md text-base font-semibold">Home</a>
      <a href="/forum" class="text-text-main hover:text-primary hover:bg-bg-card block px-3 py-2 rounded-md text-base font-semibold">Comunidade</a>
      <a href="/cart" class="text-text-main hover:text-primary hover:bg-bg-card block px-3 py-2 rounded-md text-base font-semibold">Carrinho</a>
      <% if (user?.role==='admin' ) { %>
        <a href="/admin/moderation" class="text-text-main hover:text-primary hover:bg-bg-card block px-3 py-2 rounded-md text-base font-semibold">Moderação</a>
        <a href="/admin/new-post" class="text-text-main hover:text-primary hover:bg-bg-card block px-3 py-2 rounded-md text-base font-semibold">Novo Post</a>
      <% } %>
    </div>
    <div class="pt-4 pb-4 border-t border-white/5">
      <% if (user?.role==='admin') { %>
        <div class="flex items-center px-5">
          <div class="ml-3">
            <div class="text-base font-semibold leading-none text-text-main"><%= user.email %></div>
          </div>
        </div>
        <div class="mt-3 px-2 space-y-1">
          <form method="post" action="/admin/logout">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <button class="block w-full text-left px-3 py-2 rounded-md text-base font-semibold text-text-meta hover:text-text-main hover:bg-bg-card">Sair Admin</button>
          </form>
        </div>
      <% } else { %>
        <div class="mt-3 px-2 space-y-2">
          <a href="/admin/login" class="block w-full text-center px-4 py-2 border border-white/10 rounded-lg text-base font-semibold text-text-main bg-bg-card hover:bg-bg-surface">Admin</a>
        </div>
      <% } %>
    </div>
  </div>
</nav>

